<!DOCTYPE html>
<html>
  <head>
    <title>Intercom Interview</title>
    <meta name="viewport" content="initial-scale=1.0">
    <meta charset="utf-8">
    <style>
      html, body {
        height: 100%;
        margin: 0;
        padding: 0;
      }
      #map {
        height: 500px;
      }
      .section { 
        /* a generic class uses to distuingish sections. */
        border: 1px solid black;
        padding: 40px;
        margin: 50px;
      }
      .error {
        color: red;
      }
    </style>
  </head>
  <body>
    <a href="/">Root link</a>
    <hr>
    <%= yield %>
  </body>
</html>